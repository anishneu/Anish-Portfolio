# Backend API URL for contact form (production on Netlify)
# Use https and the Render host only — do NOT include /email/send
# Example: https://your-service.onrender.com
REACT_APP_API_URL=http://localhost:5000
